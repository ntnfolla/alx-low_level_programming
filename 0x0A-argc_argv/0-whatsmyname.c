#include "main.h"
#include <stdio.h>
/**
 * main - prints my name
 * @argc: number of commands
 * @argv: array
 * Return: 0
 */
int main (int argc, char* argv[])
{
printf("%s\n", argv[argc * 0]);

return (0);
}
